a = ['a','b','c']
# a.upper()
# 반복으로 순회하면서
# 각각의 문자열에 .upper() 결과를 저장

# 시퀀스 : string, list, tuple, range
# 컬렉션 : set, dictionary

# string : 문자열 < 변경불가
# list : 중복 가능한 집합 
# tuple : 변경 불가능한 집합 < 변경불가
# range : 범위 < 변경불가

# set : 중복 불가능한 집합{}
# dictionnary : key와 values로 이뤄진 집합